spring.application.name=ResCall

# ✅ Load from environment variables (do not hardcode secrets)
spring.data.mongodb.uri=${MONGODB_URI}
jwt.secret=${JWT_SECRET}
gemini.api.key=${GEMINI_API_KEY}

# ✅ Required for Render deployment (uses dynamic port)
server.port=${PORT:8080}
